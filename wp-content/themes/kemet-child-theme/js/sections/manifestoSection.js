import {
    $horParallax
} from "../commonFunctions/horizontalParallax.js";
export function $manifestoSect() {
    //Llamado a funcion de parallax horizontal
    $horParallax('#manifesto-image-wrapper', '#manifesto-image-wrapper', '#manifesto-image', true);
}