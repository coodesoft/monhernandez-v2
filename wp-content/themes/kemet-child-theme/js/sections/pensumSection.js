import {
    $horParallax
} from "../commonFunctions/horizontalParallax.js";
export function $pensumSect() {
    //Llamado a funcion de parallax horizontal
    $horParallax('#pensum-image', '#pensum-image-wrapper', '#pensum-image', true);
}